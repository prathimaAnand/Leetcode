/* psuedocode
1. store input array as a set
2. iterate through the array
    a. check if each element as a left nieghbor, if not then it is start of the sequence, store is as a sequence
    b. check if the start of the sequence as consecutive number, if not then continue, if yes then store consecutive numbers
3. in the list of sequences return the longest one
*/